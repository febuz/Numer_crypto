# Core packages
numerapi>=2.16.0
pyspark>=3.1.2
polars>=0.19.13
h2o==3.46.0.6
h2o-pysparkling>=3.5.0
cloudpickle==2.2.1
pyarrow>=13.0.0
matplotlib>=3.7.0
scikit-learn>=1.0.2
python-dotenv>=1.0.0
numpy>=1.23.5
requests>=2.32.0
tabulate>=0.9.0
scipy>=1.15.0
psutil>=5.9.0

# GPU Monitoring
nvidia-ml-py>=11.0.0  # For NVIDIA GPU monitoring
py3nvml>=0.2.7        # Additional NVIDIA utilities
GPUtil>=1.4.0         # GPU utility library

# GPU-accelerated ML (these should be installed via conda)
xgboost>=1.6.2
lightgbm>=4.0.0

# RAPIDS Ecosystem - GPU acceleration for data processing
# These packages should be installed via conda as they have complex dependencies
cudf                # GPU DataFrame library (pandas equivalent) 
cuml                # GPU ML algorithms (scikit-learn equivalent)
cugraph             # GPU graph algorithms
cuspatial           # GPU geospatial operations
cupy                # GPU array library (numpy equivalent)
dask-cuda           # GPU task scheduling with Dask

# RAPIDS integration with Spark
rapids-4-spark      # GPU acceleration for Spark

# Note: The commented packages above should be installed via the conda
# environment setup script, not with pip. See setup_env.sh
